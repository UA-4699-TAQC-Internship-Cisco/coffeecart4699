*** Variables ***
${BROWSER}                    Chrome
${URL}                        https://coffee-cart.app/
${WINDOW_WIDTH}               1600    # Standard desktop width for test consistency
${WINDOW_HEIGHT}              1000    # Height chosen to ensure full visibility of cart UI
${SELENIUM_IMPLICIT_WAIT}     10      # Seconds to wait for element presence
